#!/usr/bin/env mshell
[uv run --with PyMuPDF python3 -c
'
import fitz
import sys
doc = fitz.open(sys.argv[1])

page_num = sys.argv[2]
if page_num == "*" or page_num.lower() == "all":
    for page in doc:
        text = page.get_text("text", sort=True)
        print(text)
else:
    page = doc[int(page_num)]
    text = page.get_text("text", sort=True)
    print(text)
'
$1 $2]!
