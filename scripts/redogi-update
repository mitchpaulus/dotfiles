#!/usr/bin/env mshell

`.gitignore` isFile not ("No .gitignore file found in the current directory" wle 1 exit) iff

[] newLines!

`.gitignore` readFile lines lines!
@lines len count!
0 i!
(
    @i @count >= (break) iff
    @lines @i nth line!
    @newLines @line append drop
    @line "# REDOGI" startsWith
    (
        @newLines ['redogi']*! lines extend drop
        break
    ) iff
    @i 1 + i!
)
loop

@newLines unlines `.gitignore` writeFile
