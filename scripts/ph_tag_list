#!/usr/bin/env mshell

[curl --silent 'https://project-haystack.org/download/defs.json']  *! parseJson "rows" get?
(
    "def" get? "val" get? as str name!
    @name "-" in @name ":" in or not
    (@name wl)
    iff
) each
