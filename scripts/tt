#!/usr/bin/env mshell
['toggl' 'projects']*! lines projs!
@projs (tsplit :1:) map sortV unlines projs!

['fzf'] @projs < * ?
not (1 exit) iff
trim proj!

"Description: " w read
not (1 exit) iff
trim desc!

['toggl' start @proj @desc]!
['toggl_set']!
