#!/usr/bin/env mshell

args (a! @a "--help" = @a "-h" = or) any if "andover2docx code.and" wl 0 exit end

$"```and\n{$1 readFile}\n```\n" codeMarkdown!

@codeMarkdown w

[andover_kde] * ! psub syntax_file!

[pandoc $"--syntax-definition={@syntax_file}"  -o $"{$1}.docx" '-'] @codeMarkdown < ;
