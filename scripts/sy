#!/usr/bin/env mshell
# Purpose: To quickly copy symbols/greek letters to the clipboard
[fzf] $DOTFILES toPath `greek_letters.txt` / < * ! wsplit :1: trim symbol!
[msh.exe $DOTFILES toPath `scripts` `clip` / /] @symbol < !
