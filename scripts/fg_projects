#!/usr/bin/env mshell
[
    'uv' 'run' '--with=requests'
    $REPOS toPath `facility-grid-api` / `fg_projects.py` /
    args
] * ! lines (tsplit) map

sortByCmp. (:-1: toFloat 0.0 maybe) 2each floatCmp -1 * end
tuw
