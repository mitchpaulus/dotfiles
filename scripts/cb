#!/usr/bin/env mshell

args (a! @a '-h' = @a '--help' = or) any
('cb - Put clipboard contents to standard output

USAGE: cb' wl
0 exit
) iff

$WSL_DISTRO_NAME?
    ( [powershell.exe Get-Clipboard] * ! "\r" "" findReplace '\n$' '' reReplace w )
    ( [xclip -o -selection clipboard] * ! w )
iff
