#!/usr/bin/env mshell

stdin lines (tsplit) map data!

@data match
    [] : "",
    [header ...rows] :
    @header map. v! $"[{@v}]" end ", " join headerContents!
    $"table.header({@headerContents})," wl

    @rows map.
        map. v! $"[{@v}]" end ", " join
    end
    ", \n" join
    wl
end
