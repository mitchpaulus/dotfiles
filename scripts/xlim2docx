#!/usr/bin/env mshell

$1 stem str ".docx" + output!
["```xlim" $1 readFile "```"] unlines input!

['pandoc' '-o' @output '--syntax-definition' $"{$HOME}/.local/share/pandoc/xlim.xml" '--highlight-style' 'kate' ] @input < !
