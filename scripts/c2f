#!/usr/bin/env mshell


[args] ['-h' '--help'] cartesian (2unpack =) any if
    "Usage: c2f <celsius>" wl 0 exit
end

args len 1 < if
    "No temperature entered" wl 1 exit
end

$1 toFloat? 9.0 * 5.0 / 32.0 + str wl
