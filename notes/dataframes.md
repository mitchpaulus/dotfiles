Group by and window are similar but different in that:

Group by/aggregate -> one row per group, collapsing

Window -> row -> collect related rows -> calculate somethings -> additional new value for row -> number of rows stays the same.
