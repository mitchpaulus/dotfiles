Project Id:

    - Project
    - Left pane, bottom, 'Settings'
        - Copy project slug/project id (UUID)

Free tier:

5 identities
Unlimited projects

```
infisical run --projectId="$PROJECT_A" --env=prod --path=/database -- ./my-command

```
