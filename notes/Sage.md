
```python
# Expand

var('x y z')
expand((x - y)^2)

expr = a + b + ..

# Collecting by a term, need to have bound expression.
expr.collect(x)
expr.coefficients(x)

latex(xxxx) # Print out expression in latex form.
```
