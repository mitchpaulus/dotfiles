# `redo`

```
exec >&2
```

```
$1: target name
$2: target name minus extension - only for default do files?
$3: temp file name - it is not absolute, so if there is a change of
directory, script must account for that.
```
