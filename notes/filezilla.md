# FileZilla

## ssh key connection on Windows

- Get Putty tools. Includes
    - Putty
    - PuttyGen (generates the public/private key pair)
    - Pageant (Putty authentication agent)

- Make sure to start Pageant - and load the desired key (should be ppk
  file).
