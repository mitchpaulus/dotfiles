```python
class FixedVariable:
    fixed = 0
    variable = 1

class Materials:
    Shaded, Shiny, Transparent, Matte = range(4)

# From standard library
from enum import Enum
class Color(Enum):
     red = 1
     green = 2
     blue = 3
```
